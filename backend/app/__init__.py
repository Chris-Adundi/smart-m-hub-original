"""Smart M Hub backend application package.

The existing API remains mounted from `server.py`. New shared utilities and
future domain routers live here so extraction can happen incrementally without
breaking route compatibility.
"""
